"""
Xử lý số tiền

Phân tích và định dạng số tiền theo quy ước Việt Nam.

Cần hiện thực:
  1. parse_money(str) -> Decimal — xử lý dấu chấm phân cách nghìn, dấu phẩy
     thập phân
  2. format_money(Decimal) -> str
  3. TUYỆT ĐỐI không dùng float cho tiền tệ
"""
