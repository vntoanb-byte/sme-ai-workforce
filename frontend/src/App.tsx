/**
 * Tệp này không còn được dùng.
 *
 * Điểm khởi động là main.tsx, và toàn bộ bố cục nằm ở
 * components/layout/AppShell.tsx. Giữ tệp lại chỉ để khỏi ai thắc mắc —
 * xoá đi cũng không ảnh hưởng gì.
 */
export {}
