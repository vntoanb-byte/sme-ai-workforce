# true  = chạy với dữ liệu giả, KHÔNG cần backend (phát triển/trình diễn giao diện)
# false = gọi backend thật qua proxy của Vite (mặc định http://localhost:8080,
#         đổi bằng biến môi trường VITE_API_TARGET khi chạy `npm run dev`)
VITE_USE_MOCK=true
